require 'spec_helper'

describe InternetWisdom do
  it 'has a version number' do
    expect(InternetWisdom::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
